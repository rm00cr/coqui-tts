"""Research helpers for the anonymization pipeline.

``utils`` holds the segment-level refinement heuristics used by ``anonymizer``'s
"refine" mode and by the root-level run scripts. Made a real package so it ships with
an install rather than only working from the repo root.
"""
